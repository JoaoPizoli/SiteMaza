services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: maza-frontend
    restart: unless-stopped
    ports:
      - "3003:3003"
    environment:
      NODE_ENV: production
      PORT: "3003"
